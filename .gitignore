node_modules
dist
build
.env
.env.local
coverage
.next
.vscode
.idea
.DS_Store
*.log
docker-compose.override.yml

# Docs, agents, and config dirs
docs/
agents/
.claude/
# .github/ is tracked — GitHub Actions needs workflows in the remote repo

# Markdown files except root README.md
*.md
!README.md

# Deploy runbook (local notes, not for the repo)
DEPLOY.md
